import { spanProperties as properties} from '../properties/span';

const span = {
    name: "Span",
    image: "icons/text_input.svg",
    nodes: ['span'],
    html: ``,
    properties
};

export default span;