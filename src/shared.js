const appName = 'UI Builder';
const version = '2.0.1';
const defaultBundledHtmlFilename = 'bundled.html';
const defaultHtmlFilename = 'index.html';
const defaultJavaScriptFilename = 'shared.js';
const defaultZipFilename = 'index.zip';
const defaultBundledHtmlFilenameTitle = 'Bundled html page with all JavaScript';
const defaultHtmlFilenameTitle = 'Separate html page without shared JavaScript';
const defaultJavaScriptFilenameTitle = 'Shared JavaScript';
const defaultZipFilenameTitle = 'Zip file with separate html and JavaScript pages';
const description = 'GUI Builder is a drag and drop ui builder to help developers create static web pages faster';
const emptyPagesTitle = 'Empty all pages?';
const emptyPagesContent = 'These pages will be permanently deleted and cannot be recovered. Are you sure?';
const components = '组件库';
const settings = '设置';
const requestErrorDialogId = 'request-error-dialog';
const errorDialogTitle = 'Error';
const requestErrorContent = 'Network error, please try again later';
const basicDialogId = 'basic-dialog';
const elementsCannotBeAlignedContent = 'Cannot align elements which are not siblings';
const multiSelectedDelete = 'mutli-selected-delete';
const multiSelectedCopy = 'multi-selected-copy';
const undoHistoryId = 'undo-history';
const undoHistoryBtnId = 'undo-history-btn';
const undoHistoryFooterId = 'undo-history-footer';
const undoHistoryDividerId = 'undo-history-divider';

module.exports = {
    appName,
    version,
    defaultHtmlFilename,
    defaultJavaScriptFilename,
    defaultZipFilename,
    description,
    defaultBundledHtmlFilename,
    defaultBundledHtmlFilenameTitle,
    defaultHtmlFilenameTitle,
    defaultJavaScriptFilenameTitle,
    defaultZipFilenameTitle,
    emptyPagesTitle,
    emptyPagesContent,
    components,
    settings,
    errorDialogTitle,
    requestErrorContent,
    requestErrorDialogId,
    basicDialogId,
    elementsCannotBeAlignedContent,
    multiSelectedDelete,
    multiSelectedCopy,
    undoHistoryId,
    undoHistoryBtnId,
    undoHistoryFooterId,
    undoHistoryDividerId
};